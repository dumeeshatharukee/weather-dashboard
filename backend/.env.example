MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/weather_db?retryWrites=true&w=majority
PORT=5000
NODE_ENV=development

# Optional: Restrict CORS to specific origins (comma-separated)
# For Railway deployment with Vercel frontend, set this in Railway Variables:
# CORS_ORIGIN=https://your-app.vercel.app,https://your-app-git-*.vercel.app
# Leave empty or unset to allow all origins (development mode)
